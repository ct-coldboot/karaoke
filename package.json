{
  "name": "karaoke",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "monitor",
    "controller"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w monitor\" \"npm run dev -w controller\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w monitor && npm run build -w controller",
    "spotify-auth": "ts-node --project tsconfig.scripts.json scripts/spotify-auth.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ts-node": "^10.9.2"
  }
}
